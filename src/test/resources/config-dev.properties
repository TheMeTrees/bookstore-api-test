# Dev environment configuration, let's pretend it differs from local

# Base URL and version for API
base.url=https://fakerestapi.azurewebsites.net
api.version=/api/v1

# Endpoints
books.endpoint=/Books
authors.endpoint=/Authors

# HTTP timeout in ms
connection.timeout=3000
socket.timeout=3000
connection.manager.timeout=3000

# Log level
log.level=DEBUG